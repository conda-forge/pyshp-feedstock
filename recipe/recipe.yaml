schema_version: 1

context:
  version: "3.1.4"

package:
  name: pyshp
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/p/pyshp/pyshp-${{ version }}.tar.gz
  sha256: 0663762be72c67684c890decd888fb8e849e01a85ee6ef0346214b7329270453

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - hatchling
    - pip
  run:
    - python >=${{ python_min }}

tests:
  - python:
      imports: shapefile
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - "*"
  - requirements:
      run:
        - python ${{ python_min }}.*
        - pytest
        - hypothesis
    files:
      source:
        - .
    script:
      - pytest -k "not (test_reader_url or test_dbf_reader_writer_roundtrip)"

about:
  summary: Pure Python read/write support for ESRI Shapefile format
  license: MIT
  license_file: LICENSE.TXT
  homepage: https://pypi.org/project/pyshp/

extra:
  recipe-maintainers:
    - pelson
    - ocefpaf
